// import React from 'react';  // this import is not neccessary, because no React-feature is used

const auxiliary = props => props.children;  // auxiliary-component just returns adjacent elements as children

export default auxiliary;